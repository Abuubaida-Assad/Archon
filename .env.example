# ARCHON Configuration

# AI Provider Configuration (Optional - falls back to deterministic rule-based architect reasoning)
OPENROUTER_API_KEY=
AI_MODEL=meta-llama/llama-3.3-70b-instruct:free
# Alternative: OPENAI_API_KEY=
# Alternative: ANTHROPIC_API_KEY=

# GitHub API Token (Optional - increases rate limits for remote repo metadata)
GITHUB_TOKEN=

# Server Port
PORT=3000
